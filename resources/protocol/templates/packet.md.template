<!-- Template for APS PROTOCOL.md v1.0. If PROTOCOL version differs, re-sync this template. -->
<!--
  This file lives at: packets/<packet_id>__v<version>/packet.md
  where <packet_id> and <version> below match the folder name.
-->
---
packet_id: REPLACE_WITH_UTC_yyyymmddThhmmssZ__short_snake_topic
# For a NEW logical packet: version=1, supersedes=null.
# For a REVISE of an existing packet: bump version, set supersedes to <packet_id>__v<previous>.
version: 1
from: REPLACE_WITH_SENDER_AGENT_ID
to: REPLACE_WITH_RECEIVER_AGENT_ID
project: REPLACE_WITH_PROJECT_SLUG
level: REPLACE_WITH_LEVEL  # L1-fyi | L2-handoff | L3-urgent
supersedes: null
created_at: REPLACE_WITH_ISO8601_UTC
ssot_refs:
  - "REPLACE: repo-relative path or anchor; remove this template line if none"
scope: "REPLACE: one-line description of what this packet covers and what it does not"
# items: snapshot at publish time. Zero items is valid for pure L1-fyi (use `items: []`).
# For multiple items, duplicate the `- id:` block below.
# status values: pending | in_progress | done | needs_clarification | fyi_align
items:
  - id: REPLACE_ID
    status: pending
    owner: REPLACE_WITH_RECEIVER_AGENT_ID
    title: "REPLACE_WITH_TITLE"
---

# REPLACE_WITH_TOPIC_HEADING

## 共同目標

REPLACE: the shared goal this handoff belongs to.

## 本方任務

REPLACE: what the sender has done or is responsible for.

## 對方任務

REPLACE: what the receiver is asked to do. Use "未確認" only when this packet is a clarification, not a normal handoff.

## 交叉點

REPLACE: where the sender and receiver work meet, and why this handoff is needed now.

## 請對方做的事

- REPLACE: concrete action item. Mirror each concrete action in YAML `items`.

## 不應誤解的事

REPLACE: boundaries, non-goals, and actions the receiver must not take.

## 真源指標

REPLACE: receiver-readable source pointers. Use shared Drive files or folders, Google Docs links, file names, versions, pages, sections, tables, dates, or APS packet references. Do not use the sender's local path as the only source.

## 接收方開工條件

REPLACE: what must be true on the receiver side before work can start.

## 風險 / 未決事項

REPLACE: open questions, blockers, or risks.

<!--
  Body is natural language, but a formal handoff should be complete enough for a stateless receiver.
  Reference items by their id when needed.
  Attachments are not part of the verified APS 0.2.x main path; use receiver-readable source pointers instead.
  After publish, this file is frozen. Revisions = a new __v2 folder with supersedes set.
-->
